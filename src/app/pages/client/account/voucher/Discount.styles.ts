import { Tabs } from "antd";
import styled from "styled-components";

export const DiscountTab = styled(Tabs)`
  .om-tabs-tabpane-active {
    padding: 8px;
    display: flex;
    gap: 20px;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
  }
`;
